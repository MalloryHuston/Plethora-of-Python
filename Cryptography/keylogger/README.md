# Keylogger Tutorial

The package ``pynput.keyboard`` contains classes for controlling and monitoring the keyboard. ```pynput``` is the *ultimate* Python library for capturing keyboard inputs – one of the coolest uses of this is making keyloggers.

## Requirements

Install the following ``pip`` packages:

```bash
pip install pynput
```

## Instructions

Run the following command in your computer's terminal:

```bash
python main.py
```

## Code Citations
`GeeksforGeeks`. `(October 17, 2025)`. `"How to use pynput to make a Keylogger?"` (Version `1.0`), <ins>[`Source code`]</ins>.
<b>https://www.geeksforgeeks.org/python/how-to-use-pynput-to-make-a-keylogger/</b>.
